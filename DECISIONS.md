# DECISIONS.md — Flippy

Registro de decisiones arquitecturales y de implementación.
Fuente de verdad junto con SPEC.md. No duplicar contenido del Spec aquí.

---

════════════════════════════════════════════════════════
📋 DECISIÓN — LLM split: Gemini 2.0 Flash (chat) + Claude 3.5 Sonnet (imágenes)
════════════════════════════════════════════════════════
Fecha: 2026-07-05
Decisión: Usar Google Gemini 2.0 Flash para respuestas RAG del chat y Anthropic Claude 3.5 Sonnet exclusivamente para análisis de imágenes adjuntas por el usuario.
Racional: Gemini 2.0 Flash ofrece menor costo por token para el volumen de consultas de chat RAG; Claude 3.5 Sonnet se mantiene para visión por su calidad en análisis multimodal combinado con corpus.
Alternativas consideradas: Claude 3.5 Sonnet para ambos casos (descartado por costo en chat de alto volumen); Gemini para ambos (descartado — análisis de imágenes con corpus requiere evaluación adicional).
Impacto: flippy-api requiere dos clientes LLM (langchain-google-genai + langchain-anthropic), dos variables de entorno (GOOGLE_API_KEY + ANTHROPIC_API_KEY), lógica de routing en el endpoint de chat según presencia de imagen adjunta.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Estructura base como monorepo de trabajo (Incremento 1)
════════════════════════════════════════════════════════
Fecha: 2026-07-05
Decisión: Scaffoldear flippy-api (FastAPI) y flippy-web (Next.js 14 App Router + TS) como carpetas dentro del mismo repo de trabajo, aunque §7 del Spec declara 2 repos GitHub privados separados.
Racional: Permite iterar rápido en un solo repo durante desarrollo; la separación en repos independientes se hace al momento del primer deploy a Railway, sin bloquear el inicio de $build.
Alternativas consideradas: crear 2 repos Git desde el día 1 (descartado por ahora — agrega fricción sin beneficio inmediato en esta etapa).
Impacto: estructura `flippy-api/` (FastAPI, health check en `/api/v1/health`, pytest configurado) y `flippy-web/` (Next.js 14, PWA manifest + service worker básico, `services/api.ts` como único punto de llamadas al backend, Jest configurado). Ambas suites de tests verdes (1 passed cada una). .gitignore actualizado para ambos servicios.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Migraciones SQL vía Session Pooler de Supabase (Incremento 2)
════════════════════════════════════════════════════════
Fecha: 2026-07-06
Decisión: Provisionar las 6 tablas de §4 (users, chats, messages, documents, document_chunks, subscriptions) e índices (IVFFlat + B-tree) contra el proyecto Supabase real del cliente, usando psycopg2 + connection string del Session Pooler, no la conexión directa.
Racional: la conexión directa de Supabase (`db.xxx.supabase.co`) requiere IPv6 — falla por DNS en redes IPv4-only (confirmado en este entorno). El Session Pooler (`aws-0-...pooler.supabase.com`) resuelve por IPv4 y soporta DDL sin restricciones en modo Session.
Alternativas consideradas: Transaction pooler (descartado — no siempre soporta DDL de forma confiable); Supabase CLI `db push` (descartado por ahora — agrega dependencia de instalación adicional sin beneficio claro en esta etapa).
Impacto: `SUPABASE_DB_URL` documentada en `.env.example` con la advertencia de usar el pooler; `scripts/apply_migrations.py` aplica migraciones en orden; `app/core/db.py` expone `get_db_connection()`; 3 tests pytest verdes (incluye conexión real y verificación de existencia de tablas) — primer test de integración real contra infraestructura del cliente.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — F-01 con Supabase Auth (GoTrue) + validación JWKS (Incremento 3)
════════════════════════════════════════════════════════
Fecha: 2026-07-09
Decisión: Implementar F-01 (registro/login) delegando la gestión de credenciales a Supabase Auth (GoTrue) en vez de un JWT propio con bcrypt. FastAPI actúa como proxy hacia `/auth/v1/*` de Supabase y valida los JWT resultantes contra el JWKS público del proyecto (ES256), no contra un secreto compartido. El rol administrador se resuelve por lista `ADMIN_EMAILS` en vez de una tabla de roles/permisos granular.
Racional: el cliente compartió una API existente (`examples/main-api`) como modelo estructural (separación router/services/model), pero esa API es multi-tenant con JWT propio (bcrypt + refresh tokens en tabla propia) y permisos granulares de un dominio distinto (clínicas estéticas) — incompatible con SPEC.md §2/§5/§11 (Flippy es single-tenant, roles simples). Entre JWT propio y Supabase Auth, se eligió Supabase Auth por ser más seguro: hashing de contraseñas, rotación de refresh tokens y (a futuro) verificación de email/reset de password quedan a cargo de Supabase en vez de código propio a mantener. Se descubrió en la práctica que el proyecto usa firma asimétrica ES256 (JWKS), no el secreto HS256 "legacy" — la validación se implementó contra JWKS, que además rota automáticamente sin gestionar un secreto.
Alternativas consideradas: JWT propio replicando el ejemplo del cliente (descartado — mayor superficie de seguridad, sin reset de password ni verificación de email); tabla de roles/permisos granular como el ejemplo (descartado — sobre-ingeniería para los 5 roles simples de §2).
Impacto: `app/integrations/supabase_auth.py` (wrapper httpx sobre GoTrue), `app/core/security.py` (validación JWKS + `require_admin`), `app/modules/auth/{model,services,router}.py`, migración 0004 (FK `users.id` → `auth.users.id`), `ADMIN_EMAILS` en `.env.example`. F-01 actualizado en SPEC.md para reflejar el flujo real. 6 tests pytest verdes contra Supabase real (registro, login, refresh, /me, password incorrecto, sin token), con limpieza automática de usuarios de prueba vía Admin API.
Hallazgos operativos resueltos en el camino: (1) SSL_CERTIFICATE_VERIFY_FAILED por interceptación TLS local — resuelto con `pip-system-certs` (usa el almacén de certificados de Windows); (2) rate limit de emails de confirmación de Supabase agotado en pruebas — resuelto desactivando "Confirm email" en Supabase Auth settings, consistente con el flujo F-01 de acceso inmediato tras registro.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
🚨 DECISIÓN DE SEGURIDAD — Habilitar RLS en las 6 tablas públicas (Incremento 3.1)
════════════════════════════════════════════════════════
Fecha: 2026-07-09
Decisión: Habilitar Row-Level Security (RLS) en users, chats, messages, documents, document_chunks y subscriptions, sin policies (deny-all para anon/authenticated vía REST).
Racional: Supabase notificó por email una vulnerabilidad crítica (rls_disabled_in_public) — con RLS deshabilitado, cualquiera con la URL del proyecto y la anon key podía leer/editar/borrar todas las filas de las 6 tablas vía la API REST pública de PostgREST. flippy-api usa exclusivamente SUPABASE_SERVICE_ROLE_KEY, que bypassea RLS por diseño de Postgres/Supabase — habilitar RLS sin policies no afecta al backend y cierra el acceso público por completo.
Alternativas consideradas: escribir policies granulares por rol antes de habilitar RLS (descartado por ahora — innecesario mientras el único cliente de estas tablas sea flippy-api con service_role; se revisará si en el futuro el frontend necesita leer Supabase directamente, lo cual hoy no ocurre por regla de arquitectura de CLAUDE.md).
Impacto: migración 0005_enable_rls.sql aplicada contra Supabase real; verificado rowsecurity=true en pg_tables para las 6 tablas; suite de tests (service_role) sigue en verde tras el cambio — 0 regresiones.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Reconstrucción de UI de chat fiel a brandbook + prototipo del cliente (Incremento 4)
════════════════════════════════════════════════════════
Fecha: 2026-07-12
Decisión: Reescribir por completo la UI de chat (construida inicialmente sin revisar docs/ a fondo) para ser fiel al brandbook formal (docs/Flipping Master - Manual de Marca.pdf) y al prototipo HTML del cliente (docs/flippy_prototipo_v4_claro (1).html), ambos presentes en el repo pero no revisados hasta este punto del desarrollo.
Racional: SPEC.md §C había sido aprobado con varios valores [proposed] por el desarrollador ante la ausencia percibida de brandbook formal. Al iniciar el incremento de UI se descubrió que sí existía un manual de marca completo y un prototipo aprobado por el cliente en docs/, con colores exactos, reglas de contraste, tipografía y estructura de pantallas (app móvil con navegación por pestañas, no el layout de escritorio con sidebar fijo construido inicialmente). Se optó por reconstruir en vez de parchear para no arrastrar una base visual incorrecta a incrementos futuros.
Alternativas consideradas: mantener el layout de escritorio ya construido y solo corregir colores (descartado por el desarrollador — se prefirió fidelidad completa al prototipo aprobado por el cliente).
Impacto: SPEC.md §C reescrito con Brand Token Sheet [explicit] (reemplaza la versión [proposed] anterior); logo real extraído del prototipo (public/icons/logo-shield.png); componentes reescritos: ChatMessage, ChatInput, ChatSidebar (+búsqueda, agrupación por fecha, footer de usuario), nuevos ChatHeader y ChatChips; layout responsive (mobile: pantallas separadas con toggle fiel al prototipo; desktop: sidebar persistente, cumple criterio §8 de testing en Chrome desktop). Verificación visual con Playwright (screenshots reales + estilos computados vía chromium headless, ya que no había chromium-cli disponible en este entorno) detectó y permitió corregir 2 bugs de CSS invisibles en los tests unitarios: (1) las custom properties --font-cormorant/--font-lato se declaraban en <body> pero --font-primary/--font-secondary las referenciaban desde :root (<html>), un nivel más arriba, causando fallback silencioso a fuentes del sistema — resuelto moviendo las clases de Next Font a <html>; (2) el <nav> del sidebar no tenía width:100%, causando que se encogiera a su contenido cuando el padre pasaba a display:flex en mobile, dejando una franja de fondo visible — resuelto agregando width:100% explícito.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Deploy a Railway como 2 servicios desde monorepo
════════════════════════════════════════════════════════
Fecha: 2026-07-13
Decisión: Deployar flippy-api y flippy-web como 2 servicios Railway separados dentro del mismo proyecto, ambos apuntando al repo único flippy en GitHub, diferenciados por Root Directory (flippy-api / flippy-web respectivamente).
Racional: consistente con la decisión del Incremento 1 de mantener el monorepo de trabajo — Railway soporta múltiples servicios desde un mismo repo vía Root Directory, evitando la fricción de separar en 2 repos GitHub distintos (previsto en §7 pero pospuesto).
Alternativas consideradas: 2 repos GitHub separados como indica §7 originalmente (pospuesto — se revisará antes de la entrega final al cliente si corresponde separar).
Impacto: flippy-api con start command `uvicorn main:app --host 0.0.0.0 --port $PORT` y variables de entorno de producción cargadas manualmente en Railway; flippy-web con `NEXT_PUBLIC_API_BASE_URL` apuntando al dominio público de flippy-api. Ambos servicios verificados end-to-end: /api/v1/health → 200, /chat → renderiza correctamente.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Pipeline de ingesta de documentos vía httpx directo (Incremento 5)
════════════════════════════════════════════════════════
Fecha: 2026-07-13
Decisión: Implementar la ingesta de documentos (F-05 backend) con wrappers httpx propios sobre Supabase Storage REST API y OpenAI embeddings REST API, en vez de instalar los SDKs oficiales (supabase-py, openai). Chunking con tiktoken (tokenización real de OpenAI) en vez de conteo aproximado por palabras/caracteres. Procesamiento en background task de FastAPI (no bloquea la respuesta al admin).
Racional: consistente con el patrón ya usado en supabase_auth.py (Incremento 3) — wrappers delgados evitan dependencias pesadas y mantienen el control total sobre errores/timeouts. tiktoken asegura que el chunking de 500 tokens (SPEC.md §5) sea exacto y no una aproximación, evitando chunks que excedan el límite real de contexto.
Alternativas consideradas: SDK oficial de OpenAI (descartado — dependencia más pesada para un solo endpoint usado); conteo de tokens por palabras (descartado — impreciso, podría generar chunks fuera de rango).
Impacto: `app/integrations/supabase_storage.py`, `app/integrations/openai_embeddings.py`, `app/modules/documents/{chunking,parsers,model,services,router}.py`. Nuevas dependencias: pdfplumber, python-docx, tiktoken, python-multipart. Límite de 20MB por archivo agregado (hallazgo propio de seguridad — no había límite antes, riesgo de DoS por upload). 11 tests pytest verdes contra Storage/OpenAI/pgvector reales.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Panel de admin sin prototipo de referencia (Incremento 6)
════════════════════════════════════════════════════════
Fecha: 2026-07-13
Decisión: Maquetar /login y /admin con los tokens de marca ya aprobados (§C) pero sin un layout de referencia del cliente, ya que el prototipo HTML solo cubre las pantallas de usuario final (inicio/chat/historial/acceso socios) y no el panel de administración.
Racional: a diferencia del Incremento 4 (donde se descartó una UI ya construida por no seguir el prototipo), acá no existe prototipo que contradecir — es una superficie interna (solo la usa el admin) sin peso de marca cliente-facing. Se prioriza funcionalidad clara sobre fidelidad visual estricta.
Alternativas consideradas: esperar a que el cliente provea un mockup del panel de admin antes de construir (descartado — bloquearía innecesariamente el avance de Hito 2; el panel de admin no es una superficie que el cliente final (socios) vea).
Impacto: app/login/page.tsx (primera pieza real de F-01 frontend — hasta ahora solo existía el backend), app/admin/page.tsx, components/AdminUploadForm.tsx, components/AdminDocumentTable.tsx, services/api.ts extendido con apiUpload/apiDelete/ApiError. Polling cada 4s mientras haya documentos en processing, sin WebSocket/SSE para esto (no lo justifica la frecuencia de uso — solo el admin sube documentos ocasionalmente). 28 tests Jest verdes (10 nuevos), build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
🚨 DECISIÓN — Fix crítico: CORS faltante bloqueaba todo el frontend (Incremento 6.1)
════════════════════════════════════════════════════════
Fecha: 2026-07-13
Decisión: Agregar CORSMiddleware a flippy-api, con origins permitidos configurables via WEB_ORIGIN (mas localhost:3000 fijo para desarrollo).
Racional: el usuario reportó error al hacer login en producción. Diagnóstico: flippy-api nunca tuvo CORSMiddleware configurado desde su creación (Incremento 1) — cualquier pedido del navegador desde flippy-web hacia flippy-api en un dominio distinto era bloqueado silenciosamente por la política de mismo origen, sin llegar siquiera a validar credenciales. Esto afectaba TODOS los endpoints, no solo login — bloqueaba también el panel de admin recién construido.
Alternativas consideradas: allow_origins=["*"] (descartado — con allow_credentials=True el spec de CORS prohíbe wildcard, y de todos modos es mas laxo de lo necesario para un solo frontend conocido).
Impacto: main.py agrega CORSMiddleware; config.py expone cors_origins (localhost:3000 + WEB_ORIGIN); WEB_ORIGIN documentado en .env.example, debe cargarse en Railway (flippy-api) con el dominio de flippy-web. 2 tests pytest nuevos (origin permitido / origin no permitido).
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Chat RAG real reemplaza el mock; gemini-2.0-flash descontinuado (Incremento 7)
════════════════════════════════════════════════════════
Fecha: 2026-07-14
Decisión: Implementar F-02 (consulta de chat RAG) de punta a punta — módulo app/modules/chat (chats/mensajes con ownership, título autogenerado), embedding de consulta + búsqueda coseno en document_chunks (top 5), prompt con historial (últimos 10 mensajes) enviado a Gemini con streaming SSE real, y wiring del frontend (ChatWindow + chat/page.tsx) contra los endpoints reales en vez de mockChatData. Usar gemini-2.5-flash en vez de gemini-2.0-flash (SPEC.md §5/§7).
Racional: al probar la integración contra la API real de Google (no solo mocks), gemini-2.0-flash devolvió 404 "no longer available" — Google lo descontinuó. Se listaron los modelos disponibles vía la API con la key real y se confirmó gemini-2.5-flash como el Flash estable vigente (GA, no preview). Se prioriza un modelo estable por sobre seguir al pie de la letra un nombre de modelo ya no soportado por el proveedor.
Alternativas consideradas: gemini-flash-latest (alias, descartado por preferir fijar una versión concreta para reproducibilidad); modelos preview (3.x) descartados por no ser aptos para producción todavía.
Hallazgo operativo: la cuenta de Google AI Studio requirió habilitar billing (free tier tenía límite 0 para este modelo) — resuelto por el desarrollador antes de poder probar en vivo.
Impacto: app/integrations/gemini.py (nuevo, streaming SSE con decodificación UTF-8 explícita a nivel de bytes — iter_lines() de httpx puede adivinar mal el encoding en fragmentos SSE pequeños), app/modules/chat/{model,services,router}.py (nuevo), api/v1/router.py registra el router. Frontend: ChatWindow.tsx consume el SSE vía apiStream y renderiza incrementalmente; chat/page.tsx reemplaza mocks por /api/v1/chats real; lib/mockChatData.ts eliminado (sin usos). F-04 (imagen) fuera de alcance — aviso explícito si se adjunta imagen. jest.setup.ts agrega polyfills de TextEncoder/TextDecoder/ReadableStream (faltantes en jsdom, necesarios para testear el parseo del stream SSE). Verificado end-to-end contra infraestructura real (login, chat, streaming, persistencia) fuera de los tests automatizados. 17 tests pytest (4 nuevos) y 34 tests Jest (7 nuevos) verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Eliminar y renombrar chat vía menú kebab (Incremento 8)
════════════════════════════════════════════════════════
Fecha: 2026-07-16
Decisión: Implementar F-03 completo (renombrar + eliminar chat), reportado por el usuario como "no puedo eliminar el chat". Backend: PATCH /api/v1/chats/{id} y DELETE /api/v1/chats/{id} en app/modules/chat, ambos validan ownership (user_id) devolviendo 404 si el chat es de otro usuario o no existe. El borrado de mensajes es cascada automática vía la FK ya existente (messages.chat_id → chats.id on delete cascade en 0002_tables.sql) — no se necesitó lógica adicional de limpieza. Frontend: ChatSidebar gana menú contextual (kebab), input de renombrado inline y modal de confirmación propio (sin librería) antes de eliminar.
Racional: investigación (systematic-debugging) confirmó que el ícono kebab del sidebar era puramente decorativo desde su introducción en el Incremento 4 — ninguna de las dos acciones tenía endpoint de backend ni handler de frontend. No era una regresión sino una funcionalidad nunca construida.
Alternativas consideradas: soft-delete (columna deleted_at) — descartado, no hay requerimiento de recuperar chats eliminados ni de retención específica para esta entidad; usar window.confirm() nativo del navegador — descartado a favor de un modal propio consistente con la identidad visual de la app.
Comportamiento del chat activo al eliminarlo (definido con el usuario): si la vista actual corresponde al chat eliminado, la pantalla queda en blanco (sin auto-seleccionar otro chat); si el usuario está viendo otro chat, no cambia nada.
Impacto: flippy-api: model.py (ChatRename), services.py (rename_chat, delete_chat), router.py (rutas PATCH/DELETE), 5 tests pytest nuevos contra Supabase real (rename propio/ajeno, delete propio/ajeno + cascada de mensajes, requiere auth). flippy-web: services/api.ts (apiPatch nuevo), ChatSidebar.tsx + .module.css (menú, input de rename, modal de confirmación), chat/page.tsx (handleRenameChat/handleDeleteChat), 3 tests Jest nuevos. SPEC.md §3 F-03 ampliado con los pasos 3-6 (antes solo mencionaba renombrar sin que existiera). 22/22 tests pytest y 37/37 tests Jest verdes, tsc sin errores.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Fix: menú kebab sin listener de clic externo (Incremento 8.1)
════════════════════════════════════════════════════════
Fecha: 2026-07-16
Decisión: Agregar un `useEffect` en ChatSidebar que registra un listener `mousedown` en `document` mientras haya un menú kebab abierto (`openMenuId`), y lo cierra si el clic ocurre fuera del `<li data-menu-id={chat.id}>` correspondiente (`Element.closest`).
Racional: el usuario reportó que el popup de opciones quedaba abierto al hacer clic en otro lugar. El menú (Incremento 8) solo tenía lógica de cierre al re-tocar el mismo kebab o al elegir Renombrar/Eliminar — nunca se contempló el clic fuera. Se usa `mousedown` (no `click`) para que el cierre ocurra antes de que la acción del elemento clickeado se dispare, evitando estados intermedios inconsistentes.
Alternativas consideradas: un ref por ítem con `useRef` — descartado por requerir un array/mapa de refs dinámico para una lista variable de chats; el atributo `data-menu-id` + `closest()` logra lo mismo sin esa complejidad.
Impacto: flippy-web/components/ChatSidebar.tsx (useEffect + atributo data-menu-id en el `<li>`), 1 test Jest nuevo (clic en document.body cierra el menú). 38/38 tests Jest verdes.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — F-04 análisis de imagen: Claude en streaming (no "llamada estándar"), bucket privado con URLs firmadas (Incremento 9)
════════════════════════════════════════════════════════
Fecha: 2026-07-17
Decisión: Implementar F-04 completo. Backend nuevo: app/integrations/anthropic_vision.py (wrapper httpx sobre la Messages API de Claude 3.5 Sonnet con streaming SSE, mismo patrón que gemini.py — sin LangChain, consistente con el precedente del Incremento 7); supabase_storage.py generalizado para aceptar un parámetro `bucket` en vez de la constante fija `documents`, más create_signed_url() nuevo; bucket privado nuevo `chat-attachments` para las imágenes que suben los usuarios en el chat; endpoint POST /{chat_id}/messages/image (multipart: file + content opcional) que sube la imagen, arma el prompt multimodal con los chunks RAG del texto adjunto (si lo hay) y transmite la respuesta de Claude en streaming, mismo formato de evento SSE que el chat de texto.
Racional (streaming): SPEC.md §7 documentaba "llamada estándar vía Anthropic SDK" para este caso (no streaming), pero CLAUDE.md establece como regla de producto "Streaming en todas las respuestas del chat (SSE)" sin excepción para imágenes. Se prioriza la regla de producto (más específica al comportamiento visible del chat) por sobre el detalle técnico de integración del documento original, y se gana consistencia: el frontend reutiliza el mismo parser de SSE para ambos casos (extraído a consumeStream en ChatWindow.tsx) en vez de tener dos rutas de UI distintas.
Racional (bucket privado + URL firmada en cada lectura, no en el upload): el bucket `documents` ya es privado por precedente (Incremento 1/5); replicar esa política para imágenes de chat evita exponer fotos de usuarios via URL pública adivinable. Guardar el path (no una URL firmada) en `messages.image_url` y resolverlo a una URL firmada de corta duración en cada `list_messages` evita que el historial se rompa cuando la firma expira — el requisito F-01 es "historial persistente... desde cualquier dispositivo" indefinidamente, una URL firmada guardada una sola vez no lo cumpliría.
Alternativas consideradas: bucket público con paths UUID no listables (descartado — mismo nivel de "seguridad por oscuridad" que ya se evitó para documentos, sin necesidad real de que sea público); firmar la URL una sola vez al subir la imagen y guardarla tal cual (descartado por la razón de expiración de arriba); usar langchain-anthropic (descartado, mismo argumento que gemini.py: un wrapper httpx delgado es más simple y ya es el patrón establecido en el proyecto).
Bloqueo de verificación: ANTHROPIC_API_KEY está vacía en el entorno — es un requisito del cliente pendiente (cuenta Anthropic con billing activo, ver documento de proyecto §9). Los tests nuevos mockean anthropic_vision.stream_vision (mismo patrón que test_send_message_streams_and_persists ya usa para Gemini); la subida a Storage y la firma de URL sí se ejecutan contra Supabase real. No hay verificación E2E contra la API real de Claude todavía — pendiente hasta que el cliente complete el requisito.
Impacto: flippy-api: anthropic_vision.py (nuevo), supabase_storage.py (bucket parametrizado + create_signed_url), chat/services.py (upload_chat_image, _resolve_image_url, build_vision_messages, constantes CHAT_IMAGES_BUCKET/SIGNED_URL_EXPIRES_IN/DEFAULT_IMAGE_CAPTION), chat/router.py (POST /{chat_id}/messages/image, límite 5 MB). flippy-web: services/api.ts (apiStreamUpload), ChatWindow.tsx (consumeStream extraído, placeholder de F-04 reemplazado). SPEC.md §3 F-04 actualizado con el detalle de bucket/streaming. 5 tests pytest nuevos (27/27 total) y 2 tests Jest nuevos (39/39 total) verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Fix proactivo: modelo Claude desactualizado (Incremento 9.1)
════════════════════════════════════════════════════════
Fecha: 2026-07-17
Decisión: Cambiar MODEL en anthropic_vision.py de claude-3-5-sonnet-20241022 (el id que especifica SPEC.md §7, sin fecha de revisión) a claude-sonnet-5.
Racional: al correr $verify tras cerrar el Incremento 9, una búsqueda del lineup vigente de Anthropic (julio 2026) confirmó que el roster actual es Fable 5 / Opus 4.8 / Sonnet 5 / Haiku 4.5 — ningún resultado listó claude-3-5-sonnet-20241022 como modelo activo. Un id de octubre 2024 sin verificar contra una API que ya retiró gemini-2.0-flash en circunstancias casi idénticas (Incremento 7) es un riesgo conocido, no hipotético: sin este fix, F-04 fallaría con 404 en el primer uso real, exactamente como pasó con Gemini.
Diferencia importante con el fix de Gemini: aquella corrección se hizo después de un 404 real contra la API en vivo. Esta se hace sin key configurada — es una corrección preventiva basada en evidencia externa (WebSearch) y en el roster de modelos del propio contexto de sesión, no una confirmación directa. Se documenta explícitamente como pendiente de reconfirmación una vez el cliente complete el requisito de cuenta Anthropic con billing (ver §9 del documento de proyecto).
Alternativas consideradas: dejar claude-3-5-sonnet-20241022 y esperar a que falle en vivo para corregir (descartado — ya se conoce el patrón de falla exacto por el caso Gemini, no tiene sentido repetir el mismo error evitable); usar un alias tipo "claude-latest" si existiera (no se encontró evidencia de que la API de Anthropic ofrezca ese alias, a diferencia de Gemini con gemini-flash-latest).
Impacto: flippy-api/app/integrations/anthropic_vision.py (constante MODEL + comentario explicando la desviación). SPEC.md §7 (sección Anthropic reescrita con la desviación documentada) y §A Delta Log (entrada 5). Sin cambios de tests (el modelo no se ejercita en los tests mockeados).
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Verificación E2E real de F-04 + system prompt de visión separado (Incremento 9.2)
════════════════════════════════════════════════════════
Fecha: 2026-07-18
Decisión: Con ANTHROPIC_API_KEY ya configurada por el cliente, se corrió una verificación en vivo completa de F-04 (script ad-hoc, no parte de la suite): llamada directa a anthropic_vision.stream_vision, luego el endpoint POST /{chat_id}/messages/image completo sin mocks. Se encontró que el endpoint reutilizaba SYSTEM_PROMPT (el del chat de texto RAG) para la llamada a Claude, y ese prompt instruye "Respondé UNICAMENTE basandote en el contexto provisto" — con una imagen sin contexto de corpus relacionado, Claude se negaba a describirla ("no tengo información en el contexto"). Se agregó VISION_SYSTEM_PROMPT, un prompt separado en chat/services.py que indica a Claude analizar la imagen siempre e integrar el contexto del corpus solo cuando sea relevante.
Racional: F-04 (SPEC.md §3) pide explícitamente "Claude analiza la imagen combinándola con los chunks RAG recuperados" — un prompt que bloquea la respuesta cuando no hay chunks relevantes contradice ese requisito. Reusar el mismo SYSTEM_PROMPT para dos tareas con objetivos distintos (responder solo desde el corpus vs. analizar una imagen) era la causa raíz; separarlos es la corrección mínima sin tocar el comportamiento ya validado del chat de texto.
Cómo se encontró: no fue evidente en los tests unitarios (mockean stream_vision, así que nunca ejercitan el contenido real del prompt contra el modelo). Solo apareció al hacer la verificación E2E real pedida explícitamente antes de dar F-04 por cerrado — confirma el valor de no marcar una feature como terminada hasta probarla contra infraestructura real cuando esta disponible.
Alternativas consideradas: ajustar el SYSTEM_PROMPT único para cubrir ambos casos con condicionales en el texto (descartado — dos prompts simples y explícitos son más fáciles de razonar y mantener que uno con lógica condicional embebida).
Verificación realizada: (1) llamada directa a stream_vision con imagen real 64×64 (patrón tablero de ajedrez) — confirma modelo, formato SSE y schema de bloque de imagen correctos; (2) endpoint completo sin mocks, con y sin caption de texto — en ambos casos Claude describe la imagen correctamente tras el fix; (3) suite completa de pytest (27/27) sigue en verde tras el cambio de prompt.
Impacto: flippy-api/app/modules/chat/services.py (VISION_SYSTEM_PROMPT nuevo), chat/router.py (usa VISION_SYSTEM_PROMPT en vez de SYSTEM_PROMPT para el endpoint de imagen). SPEC.md §7 y §13 actualizados con el resultado de la verificación real.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Íconos PWA generados con Pillow desde el logo existente, fondo de marca (Incremento 10)
════════════════════════════════════════════════════════
Fecha: 2026-07-19
Decisión: Generar icon-192.png, icon-512.png y apple-touch-icon.png con un script Pillow ad-hoc a partir de public/icons/logo-shield.png (207×245, no cuadrado), centrado sobre un canvas cuadrado con el color de fondo de marca #F4F1EC y ~70-72% de escala (deja zona segura para íconos maskable). manifest.json actualizado con purpose: "any maskable" en ambas entradas.
Racional: manifest.json ya declaraba estos dos archivos desde el Incremento 1, pero nunca se generaron — la PWA técnicamente no era instalable con ícono propio (fallback a ícono genérico del navegador). No había un asset de icono cuadrado en docs/ ni en el prototipo HTML del cliente; logo-shield.png (extraído del prototipo en el Incremento 4) ya es un mark autocontenido (fondo vino + marca blanca), así que generar las variantes cuadradas desde ahí es la opción más fiel a la marca aprobada, sin inventar un diseño nuevo.
Alternativas consideradas: pedir al cliente assets de ícono dedicados antes de continuar (descartado por ahora — el gap bloqueaba la instalabilidad de la PWA, un criterio de aceptación de Hito 1; generar una versión razonable desde el asset ya aprobado es preferible a dejarlo roto mientras se espera); fondo transparente en vez de #F4F1EC (descartado — un ícono de home screen con fondo transparente se ve mal sobre fondos de sistema arbitrarios, mejor un fondo sólido de marca).
Impacto: flippy-web/public/icons/{icon-192,icon-512,apple-touch-icon}.png (nuevos), manifest.json (purpose maskable). Si el cliente entrega un ícono oficial más adelante, reemplazar estos archivos sin tocar el resto del pipeline.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Banner de onboarding iOS global en el root layout (Incremento 10)
════════════════════════════════════════════════════════
Fecha: 2026-07-19
Decisión: Nuevo componente IOSInstallBanner.tsx, montado en app/layout.tsx (aplica a toda la app, no solo /chat). Detecta iOS Safari no instalado combinando userAgent (iPhone/iPad/iPod, más el caso iPadOS 13+ que se reporta como "Macintosh" con maxTouchPoints > 1) con navigator.standalone / matchMedia('(display-mode: standalone)'). Dismisseable, con persistencia en localStorage (no vuelve a aparecer tras cerrarlo).
Racional: F-08 punto 1 de SPEC.md ya especificaba este banner ("Safari → Compartir → Agregar a pantalla de inicio") desde el documento original, pero nunca se había construido — gap real detectado en la auditoría de Hito 1, no una desviación de alcance.
Alternativas consideradas: mostrarlo solo dentro de /chat (descartado — /login es probablemente la primera pantalla que ve un usuario nuevo en iPhone, más relevante mostrarlo ahí también); usar una librería de detección de PWA-installability (descartado — la detección necesaria es una condición simple, no justifica una dependencia nueva).
Impacto: flippy-web/components/IOSInstallBanner.tsx + .module.css (nuevos), app/layout.tsx (montado + metadata.icons.apple + metadata.appleWebApp + viewport.themeColor vía Next.js Metadata API). 4 tests Jest nuevos (43/43 total) verdes; verificado en el HTML de next build que los meta tags apple-mobile-web-app-capable/apple-touch-icon/theme-color están presentes.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Pantalla de registro: toggle sobre /login, no ruta nueva (Incremento 11)
════════════════════════════════════════════════════════
Fecha: 2026-07-19
Decisión: Agregar un toggle Ingresar/Registrate al mismo componente login/page.tsx (mismos campos email+contraseña, cambia solo el endpoint llamado: /auth/login vs /auth/register) en vez de crear una ruta /register separada.
Racional: se detectó el gap armando la checklist de pruebas manuales para el desarrollador — el usuario preguntó "¿cómo agrego un usuario de prueba?" y no había forma de hacerlo desde la app. Antes de construir, se confirmó contra SPEC.md §2 (roles) y §3 (F-01) que el modelo de producto es autoregistro abierto: "Usuario gratuito = miembro registrado sin suscripción activa", y el rol Administrador solo tiene alcance sobre el corpus de documentos, no sobre altas de usuarios. No hay ninguna mención en el documento de proyecto original ni en SPEC.md de un flujo de invitación o alta por admin en hitos posteriores — se descartó esa hipótesis con el desarrollador antes de implementar.
Por qué toggle y no ruta separada: los dos formularios son idénticos (mismos dos campos), y el backend ya devuelve el mismo TokenResponse para ambos endpoints — separar en dos páginas hubiera duplicado el layout de la card sin ganar nada; un toggle de estado es la opción más simple que cumple el requisito.
Alternativas consideradas: ruta /register dedicada (descartada por la duplicación de código sin beneficio real); dejarlo pendiente para confirmar con Virgilio primero (descartada por el desarrollador — el modelo de autoregistro ya está documentado y aprobado en el SPEC vigente, no había ambigüedad real que justificara la pausa).
Impacto: flippy-web/app/login/page.tsx (toggle de modo, manejo de error 422 para email duplicado), page.module.css (estilo del link de toggle). SPEC.md §3 F-01 actualizado con el paso 1 (toggle) y el paso 6 (aclaración de autoregistro abierto). 4 tests Jest nuevos (46/46 total) verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — OD-01 resuelta: límite del plan gratuito = 6 meses desde el alta [LOCK]
════════════════════════════════════════════════════════
Fecha: 2026-07-23
Decisión: El plan gratuito otorga acceso completo a la IA durante los primeros 6 meses desde `users.created_at`. Cumplida esa ventana sin que el usuario haya iniciado una suscripción paga, el acceso se bloquea por completo — debe iniciar el flujo de suscripción (F-06) para recuperarlo. Nueva RN-07 en SPEC.md §6.
Racional: OD-01 quedaba abierta desde el Spec inicial (¿el límite era por mensajes/día, mensajes/mes o corpus reducido?). Virgilio confirmó, vía el desarrollador, que el límite es de tipo temporal: 6 meses desde el alta, con bloqueo total (no degradación parcial) al vencer.
Alternativas consideradas: límite por cantidad de mensajes/día o mes (descartado por el cliente); acceso degradado pero no bloqueado tras el vencimiento (descartado explícitamente — el cliente eligió bloqueo total, el usuario debe suscribirse para recuperar acceso).
Impacto: SPEC.md §2 (tabla de roles), §6 (RN-01 actualizada + RN-07 nueva), §12 (OD-01 marcada resuelta, nueva OD-03 sobre la interacción con el estado `cancelado` de usuarios que sí llegaron a pagar), §A (delta #7), §13. Sin cambios de código todavía: el enforcement (chequeo de `created_at` contra la ventana de 6 meses, vía job programado o verificación en el momento de la consulta) no está implementado — queda para el incremento de Mercado Pago (F-06/F-07), que tampoco existe aún en el código (`flippy-api/app/modules` no tiene módulo de suscripciones/webhooks).
Decisión abierta derivada (OD-03, no bloqueante para este cambio): un usuario que sí pagó y luego cancela (RN-03) hoy conserva "acceso solo plan gratuito" — falta confirmar si queda sujeto a su ventana original de 6 meses desde el alta (probablemente ya vencida) o si recibe una ventana nueva desde la cancelación. Bloquea el diseño final de F-06/F-07, no bloquea esta decisión.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Carpetas y subcarpetas de documentos: anidamiento ilimitado, borrado bloqueado si no está vacía (Incremento 12)
════════════════════════════════════════════════════════
Fecha: 2026-07-24
Decisión: Nueva tabla `document_folders` self-referencing (`parent_id` nullable → anidamiento ilimitado, sin límite de niveles fijo) y `documents.folder_id` nullable (null = raíz del corpus). Una carpeta con documentos o subcarpetas no puede eliminarse — la API devuelve 409 con mensaje explícito; el admin debe vaciarla primero (mover o borrar su contenido). Las carpetas son puramente organizativas: no tocan el pipeline de ingesta/RAG (chunking, embeddings, retrieval funcionan igual sin importar la carpeta del documento).
Racional: pedido directo del desarrollador para ordenar el corpus a medida que crece. Anidamiento ilimitado elegido sobre un límite fijo de 2 niveles porque el modelo self-referencing cuesta lo mismo de implementar y es más flexible a futuro. Bloquear el borrado de carpetas no vacías (en vez de cascada o reubicación automática) prioriza evitar el borrado accidental de documentos del corpus sobre la conveniencia de un borrado más rápido — ambas alternativas fueron evaluadas y descartadas explícitamente con el desarrollador antes de implementar.
Alternativas consideradas: anidamiento a 2 niveles fijos (descartado — mismo esfuerzo que ilimitado, menos flexible); eliminación en cascada de una carpeta no vacía (descartado — riesgo de borrar contenido del corpus sin querer); mover el contenido a la carpeta padre/raíz al eliminar (descartado — puede generar una raíz desordenada con el tiempo, y el desarrollador prefirió el control explícito de "vaciar antes de borrar").
Impacto: migración `0007_document_folders.sql` (aplicada contra Supabase real, incluye `on delete restrict` en ambos FK como backstop de la regla a nivel DB, además del chequeo explícito en el service que da un 409 con mensaje claro en vez de depender del error crudo de la FK). Backend: `FoldersService` nuevo en `app/modules/documents/services.py`, endpoints `/admin/folders` (CRUD) y `PATCH /admin/documents/{id}/folder` en `router.py`, `document_folders_router` registrado en `app/api/v1/router.py`. Frontend: `AdminFolderPanel.tsx` (breadcrumb + grid de subcarpetas + crear/renombrar/eliminar inline) nuevo, `AdminDocumentTable` extendido con selector de carpeta por fila (prop `onMove` opcional, no rompe usos existentes), `admin/page.tsx` reescrito para manejar navegación por carpeta + toggle "ver todo el corpus". `apiUpload` en `services/api.ts` generalizado para aceptar campos de formulario extra. SPEC.md §1 sin cambios, §3 (F-05 ampliado), §4 (tabla nueva + FK), §6 (RN-08 nueva), §A (delta #8), §13 actualizados. 31/31 tests pytest y 52/52 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Navegación de carpetas rediseñada: árbol lateral en vez de breadcrumb + grid (Incremento 12.1)
════════════════════════════════════════════════════════
Fecha: 2026-07-24
Decisión: Reemplazar el `AdminFolderPanel` de breadcrumb + grid de tarjetas (Incremento 12) por un árbol vertical recursivo en un sidebar propio de 220px, entre el nav de administración y el panel principal. Expandir/contraer por carpeta con chevron, auto-expande la cadena de ancestros de la carpeta actualmente abierta (para que navegar profundo nunca deje el árbol colapsado alrededor de la selección), ítem activo resaltado con los tokens de marca (`--color-primary` sobre fondo tenue). El nombre de la carpeta actual pasa a ser el título del panel principal, reemplazando el breadcrumb.
Racional: el desarrollador compartió una referencia visual de un file manager (sidebar izquierdo con árbol Home/Files/Starred/etc., tabla de archivos a la derecha) y pidió ese patrón específicamente para navegar carpetas. Se adaptó la estructura (sidebar de árbol + tabla) pero no la paleta del ejemplo (lavanda/violeta) — el Brand Token Sheet de §C (vino/marfil/Cormorant Garamond) ya está aprobado por el cliente y es de alcance fijo, no se reabre por una referencia de otro producto.
Alternativas consideradas: mantener el breadcrumb + grid del Incremento 12 y solo mover el bloque a la izquierda (descartado — no cumple con lo pedido: un file manager clásico navega por árbol expandible, no por grid de tarjetas); sidebar con acordeón de un solo nivel (descartado — no soporta bien el anidamiento ilimitado ya decidido en el Incremento 12).
Impacto: `AdminFolderPanel.tsx` y su CSS reescritos (mismo nombre de archivo — es una evolución del mismo componente, no uno nuevo); `admin/page.tsx` pasa de grid de 2 a 3 columnas (`280px 220px 1fr`) y el `<h1>` del panel principal muestra el nombre de la carpeta actual; tests actualizados/agregados en `AdminFolderPanel.test.tsx` (auto-expand de ancestros, expandir vía chevron sin navegar). 53/53 tests Jest verdes, build de producción limpio. Sin cambios de backend — la API de carpetas del Incremento 12 no se tocó.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Top bar fija, búsqueda global entre carpetas e íconos SVG (Incremento 12.2)
════════════════════════════════════════════════════════
Fecha: 2026-07-24
Decisión: Tres ajustes puntuales sobre `/admin` pedidos por el desarrollador. (1) Barra superior fija fuera del grid de 3 columnas con el título "Documentos" + nombre de la carpeta actual, reemplazando el `<h1>` que vivía dentro del panel principal. (2) `admin/page.tsx` deja de refetchear por carpeta cada vez que se navega — carga el corpus completo una sola vez (`GET /admin/documents?all=true`) y filtra por `folder_id` en el cliente con `useMemo`; esto habilita pasarle todo el corpus a `AdminDocumentTable` como `searchScope`, que la tabla usa para buscar por nombre en TODO el corpus (no solo la carpeta abierta) apenas el campo de búsqueda tiene texto, con un aviso visible "Buscando en todo el corpus, no solo en esta carpeta". El checkbox "Ver todo el corpus" del incremento anterior se eliminó por redundante. (3) Los íconos de carpeta pasan de emoji (📁 para carpetas, 🏠 para Raíz) a SVG monoline (`FolderIcon`, `RootIcon`, `stroke="currentColor"`), consistentes con el resto de los íconos de la app (`AdminSidebar`, `ChatHeader`).
Racional: el desarrollador compartió una referencia visual de file manager y pidió explícitamente estos tres puntos: barra superior con el nombre de la sección, cuerpo más simple (solo tabla, sin controles redundantes), posibilidad de buscar en todas las carpetas, e íconos más modernos. Fetch único + filtrado client-side se eligió sobre mantener el refetch por carpeta porque es la forma más simple de habilitar búsqueda global sin duplicar llamadas a la API ni manejar dos fuentes de verdad (lista de carpeta vs. lista completa).
Alternativas consideradas: mantener el checkbox "Ver todo el corpus" además de la búsqueda global (descartado — redundante, dos maneras de llegar al mismo resultado contradice "cuerpo más simple"); refetchear la carpeta actual y hacer una llamada aparte a `all=true` solo cuando hay texto de búsqueda (descartado — más llamadas de red y una condición de carrera extra sin necesidad, dado que el corpus completo ya cabe cómodamente en memoria para este volumen de documentos).
Impacto: `admin/page.tsx` reescrito (estado `allDocuments` único, `documentsInCurrentFolder` derivado con `useMemo`, sin estado `viewAll`); `page.module.css` con `.page`/`.topbar`/`.topbarTitle`/`.topbarFolder` nuevos, `.layout` pasa a `flex:1` dentro de `.page` en vez de `height:100vh` propio; `AdminDocumentTable.tsx` con prop `searchScope` opcional y aviso de alcance de búsqueda, ya no oculta el buscador cuando la carpeta actual está vacía (antes retornaba el empty-state antes de renderizar la barra de búsqueda); `AdminFolderPanel.tsx`/`.module.css` con `FolderIcon`/`RootIcon` SVG reemplazando los emoji. 55/55 tests Jest verdes (4 nuevos), build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Top bar de /admin igualada visualmente a la de /chat (Incremento 12.3)
════════════════════════════════════════════════════════
Fecha: 2026-07-25
Decisión: Reemplazar la barra superior custom del Incremento 12.2 (`<h1>` "Documentos" + nombre de carpeta con separador "/") por `AdminTopBar.tsx`, un componente nuevo con el mismo layout y CSS que `ChatHeader.tsx` (escudo del logo + "Flippy" en tipografía primaria + línea de estado con punto de color y texto pequeño), pero sin el botón de historial (no aplica fuera del chat) y con el texto de estado como "Documentos" (o "Documentos · {carpeta actual}" cuando hay una carpeta abierta) en vez de "Asistente · activo".
Racional: el desarrollador pidió explícitamente que la barra fuera "igual que en la sección chat", no una variante propia — la referencia visual que compartió mostraba ese layout de header (escudo + nombre + status) reutilizado literalmente entre secciones de la app, cambiando solo el texto de estado.
Alternativas consideradas: reutilizar `ChatHeader.tsx` directamente pasándole el texto por prop (descartado — `ChatHeader` tiene el botón de historial de chats hardcodeado con su propio `onOpenHistory`, y forzar props opcionales para ocultarlo en `/admin` complica un componente que hoy es simple; un componente nuevo con el mismo CSS es más claro que ramificar `ChatHeader` para dos contextos distintos).
Impacto: `AdminTopBar.tsx` + `AdminTopBar.module.css` nuevos (mismo CSS que `ChatHeader.module.css`, sin `.historyButton`); `admin/page.tsx` usa `<AdminTopBar currentFolderName={...} />` en vez del `<header>` inline; `page.module.css` sin las clases `.topbar`/`.topbarTitle`/`.topbarFolder` del incremento anterior (ya no se usan). 2 tests Jest nuevos en `AdminTopBar.test.tsx` (57/57 total), build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Fix: AdminTopBar no debe cubrir todo el ancho de la página (Incremento 12.4)
════════════════════════════════════════════════════════
Fecha: 2026-07-25
Decisión: Mover `AdminTopBar` de fuera del grid de 3 columnas (Incremento 12.3, cubría nav admin + árbol de carpetas + contenido) a adentro de `<main>`, arriba de la tabla — mismo patrón estructural que `ChatWindow`, donde `ChatHeader` vive dentro de la columna central y no arriba de `ChatSidebar`.
Racional: el desarrollador compartió una captura de `/chat` mostrando que ahí el header NO ocupa todo el ancho — solo la columna de contenido, dejando el sidebar de chats a la izquierda sin header propio. El Incremento 12.3 había asumido "igual que el chat" como "mismo layout visual del header" pero no replicó correctamente su posicionamiento en el árbol de componentes; esta corrección alinea ambos aspectos.
Alternativas consideradas: ninguna — es una corrección directa de posicionamiento, no una decisión de diseño nueva.
Impacto: `admin/page.tsx` — `AdminTopBar` pasa a ser hijo de `<main>` (antes de `.mainContent`, el nuevo contenedor scrolleable que agrupa upload form + tabla); `page.module.css` — se eliminó el wrapper `.page` (flex column) del Incremento 12.2, `.layout` vuelve a ser el grid de `height:100vh` de nivel superior, `.mainPane` pasa a `display:flex; flex-direction:column` con `.mainContent` como área de scroll (mismo patrón que `.window`/`.messages` de `ChatWindow.module.css`). 57/57 tests Jest siguen en verde (cambio de posicionamiento, no de comportamiento — sin tests nuevos), build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — El header de /admin se extiende también sobre el árbol de carpetas (Incremento 12.5)
════════════════════════════════════════════════════════
Fecha: 2026-07-25
Decisión: `AdminTopBar` deja de estar limitado a la columna de contenido (`<main>`) — ahora cubre tanto `AdminFolderPanel` como `mainPane`. Solo `AdminSidebar` (el nav de administración, columna más a la izquierda) queda sin header propio arriba. Layout reestructurado: el grid externo pasa de 3 columnas planas (`280px 220px 1fr`) a 2 (`280px 1fr` — nav admin | resto), donde "resto" es una columna flex (`.rightPane`) con el header arriba y una fila (`.contentRow`) debajo que acomoda el árbol de carpetas (220px) y el contenido (1fr) lado a lado.
Racional: pedido explícito del desarrollador — el header "Documentos" debe leerse como el encabezado de toda la sección de gestión documental (carpetas + tabla), no solo de la tabla. Coherente con que las carpetas son parte del mismo concepto de "Documentos" que el header nombra, mientras que el nav de administración es un elemento de navegación de más alto nivel (podría eventualmente tener otras secciones además de Documentos) y por eso queda fuera del alcance visual del header.
Alternativas consideradas: ninguna — ajuste directo de alcance visual pedido por el desarrollador tras ver el resultado del Incremento 12.4.
Impacto: `admin/page.tsx` reestructurado (grid de 2 columnas + `.rightPane`/`.contentRow` nuevos envolviendo `AdminTopBar` + árbol de carpetas + contenido); `page.module.css` con `.rightPane`, `.contentRow` nuevos, `.mainPane` simplificado (ya no necesita su propio `.mainContent` de scroll interno separado del header, porque el header ahora vive un nivel más arriba). Sin cambios en `AdminTopBar.tsx`, `AdminFolderPanel.tsx` ni `AdminDocumentTable.tsx` — solo composición del layout. 57/57 tests Jest siguen en verde (cambio de layout, no de comportamiento), build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Header sin logo/marca + título más grande; color propio para la columna de Carpetas (Incremento 12.6)
════════════════════════════════════════════════════════
Fecha: 2026-07-25
Decisión: `AdminTopBar` deja de imitar a `ChatHeader` (escudo + "Flippy" + línea de estado) — pasa a mostrar únicamente "Documentos" como título grande (`<h1>`, 1.75rem), con el nombre de la carpeta actual al lado cuando corresponde. `AdminFolderPanel` cambia su fondo de `--color-white` a `--color-surface-alt` (Gris Niebla), el mismo tono que ya usa `AdminSidebar` para la columna de navegación de admin.
Racional: el desarrollador pidió explícitamente sacar el logo/marca del header y agrandar el texto "Documentos" — el parecido con `ChatHeader` (Incremento 12.3) ya no era lo buscado, era un paso intermedio. El color de fondo distinto en Carpetas separa visualmente esa columna de navegación del contenido blanco de la tabla, consistente con el patrón ya existente de `AdminSidebar` (nav = gris, contenido = blanco) en vez de introducir un tercer tono.
Alternativas consideradas: mantener el layout de `ChatHeader` y solo agrandar el texto (descartado — el pedido fue explícito en sacar logo y nombre, no solo el tamaño); usar un tono de vino tenue para Carpetas en vez del gris de `AdminSidebar` (descartado por ahora — reutilizar el gris ya aprobado mantiene el patrón "nav = gris" consistente en las dos columnas de navegación de `/admin`, en vez de sumar un tercer color sin pedido explícito de cuál).
Impacto: `AdminTopBar.tsx` reescrito (sin `<Image>` del logo, sin el div `.identity`/`.status`/`.dot`); `AdminTopBar.module.css` reescrito (`.title` en vez de `.name`/`.status`); `AdminFolderPanel.module.css` — `.wrapper` background a `--color-surface-alt`. `AdminTopBar.test.tsx` actualizado (ya no busca el string combinado "Documentos · Presupuestos", busca "Documentos" y el nombre de la carpeta como nodos separados). 57/57 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Fix de peso de fuente heredado + Carpetas usa el color del body (Incremento 12.7)
════════════════════════════════════════════════════════
Fecha: 2026-07-25
Decisión: El desarrollador reportó que "Documentos" no se veía con "la misma letra" que "Flippy Admin" pese a que ambos ya usaban `var(--font-primary)` — la causa real era `globals.css: h1, h2, h3 { font-weight: var(--font-weight-bold) }`, una regla global que pone en negrita cualquier `<h1>`/`<h2>`/`<h3>` del sitio; `.title` de `AdminTopBar` es un `<h1>`, mientras que `.brandName` de `AdminSidebar` es un `<span>` (no afectado por esa regla, hereda `font-weight: regular` del body). Mismo family, distinto weight — de ahí la percepción de "letra distinta". Fix: `.title` fija `font-weight: var(--font-weight-regular)` explícitamente, sobrescribiendo la regla global heredada. Separado, `AdminFolderPanel.wrapper` pasa de `--color-surface-alt` a `--color-bg` (el mismo color que usa `body`), a pedido del desarrollador — Carpetas ahora se funde con el fondo general de la página en vez de leerse como un bloque gris propio.
Racional: diagnosticar la causa raíz (regla CSS global de h1/h2/h3) en vez de forzar un ajuste cosmético a ciegas evita que el mismo bug reaparezca en otros `<h1>` nuevos del panel de admin. El color de Carpetas es una preferencia visual directa del desarrollador tras ver el gris del Incremento 12.6 en la app real.
Alternativas consideradas: cambiar `.brandName` a `<h1>` en vez de bajar el peso de `.title` (descartado — un nav de sidebar no es semánticamente un heading de página, y hubiera düplicado el problema en sentido inverso); quitar el `font-weight: bold` de la regla global `h1, h2, h3` (descartado — esa regla es usada intencionalmente en otras páginas del sitio para dar peso a títulos reales, tocar el global hubiera arriesgado romper esos casos por un fix puntual de `/admin`).
Impacto: `AdminTopBar.module.css` — `.title` con `font-weight: var(--font-weight-regular)` explícito; `AdminFolderPanel.module.css` — `.wrapper` background a `--color-bg`. Sin cambios de comportamiento, sin tests nuevos (los 2 tests de `AdminTopBar.test.tsx` ya verificaban el texto, no el peso visual). 57/57 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — AdminTopBar igualado al ChatHeader en alto y tamaño de título (Incremento 12.8)
════════════════════════════════════════════════════════
Fecha: 2026-07-25
Decisión: `AdminTopBar.module.css` copia los valores exactos de `ChatHeader.module.css` en los dos puntos pedidos: `padding: 16px 15px 13px` (define el alto del header, antes `20px 24px`) y `.title` a `16px` (antes `1.75rem` ≈ 28px, ahora el mismo tamaño que `.name` "Flippy" en el chat). `.folder` (nombre de la carpeta actual) se redujo a `var(--font-size-label)` para no quedar más grande que el propio título tras el cambio.
Racional: pedido explícito y literal del desarrollador — "mismo alto que el header del chat" y "mismo tamaño y estilo que la palabra Flippy" — se tradujo en copiar los valores numéricos exactos de `ChatHeader`, no una aproximación visual.
Alternativas consideradas: ninguna — instrucción numérica directa, sin ambigüedad de diseño que arbitrar.
Impacto: `AdminTopBar.module.css` — `.header` (`padding`, `align-items: center` en vez de `baseline` ya que ambos elementos quedan chicos y no necesitan alineación por línea base), `.title` (`font-size: 16px`), `.folder` (`font-size: var(--font-size-label)`). Sin cambios en `AdminTopBar.tsx` ni en los tests existentes — ajuste puramente de estilos. 57/57 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — AdminTopBar vuelve a replicar ChatHeader completo, con logo (Incremento 12.9)
════════════════════════════════════════════════════════
Fecha: 2026-07-25
Decisión: `AdminTopBar` deja de ser un `<h1>` + carpeta suelto y vuelve a la estructura completa de `ChatHeader` — escudo del logo + nombre + línea de estado con punto de color — pero con el texto adaptado al contexto de `/admin`: "Flippy" → "Documentos" (nombre), "Asistente · activo" → "Panel de administración" (estado por defecto) o "Carpeta: {nombre}" cuando hay una carpeta abierta (reemplaza al estado, no lo acompaña — mismo patrón de una sola línea de estado que ya usa `ChatHeader`).
Racional: el desarrollador pidió explícitamente "igual que el header del chat con el ícono incluido" — instrucción directa de restaurar el logo que se había sacado en el Incremento 12.6 por un pedido anterior distinto ("sin el logo"). Los pedidos de diseño de esta sección fueron iterando (con logo → sin logo → con logo de nuevo); se prioriza siempre la instrucción más reciente del desarrollador sin cuestionar el ida y vuelta.
Alternativas consideradas: ninguna — instrucción directa y explícita, sin ambigüedad de diseño que arbitrar.
Impacto: `AdminTopBar.tsx` reescrito (vuelve el `<Image>` del logo + estructura `.identity`/`.name`/`.status`/`.dot`, ya no `<h1>` + `.folder` sueltos); `AdminTopBar.module.css` reescrito como réplica exacta de `ChatHeader.module.css` (mismos valores de padding/tamaños/colores, sin `.historyButton`). `AdminTopBar.test.tsx` actualizado al nuevo texto ("Documentos" + "Panel de administración" / "Carpeta: {nombre}"). 57/57 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Filas de carpetas como tarjetas con borde, indentación por margen no por padding (Incremento 12.10)
════════════════════════════════════════════════════════
Fecha: 2026-07-25
Decisión: `AdminFolderPanel` pasa de filas planas (fondo transparente, solo hover sutil) a tarjetas individuales: borde `1px solid var(--color-line)` con `border-radius:var(--border-radius-md)`, fondo blanco, que se resaltan en `var(--color-primary)` al hover o cuando la carpeta está activa (reemplaza el fondo tenue anterior). Se agregó un punto de estado verde (`--color-pine`) al final de cada fila. Los nombres de carpetas de nivel raíz (depth 0) se muestran en mayúscula y negrita. La indentación por profundidad se recalculó: en vez de `padding-left` creciente dentro de una tarjeta de ancho fijo (que dejaba el borde siempre en el mismo lugar, solo corriendo el contenido adentro), pasa a `margin-left` en el `<li>` contenedor — con eso la tarjeta en sí se angosta y se corre a la derecha en cada nivel, con los bordes derechos alineados entre todos los niveles, igual que la referencia compartida por el desarrollador. No se agregó ninguna línea de conexión vertical entre niveles — la implementación previa tampoco la tenía, así que ese punto de la referencia ya estaba cumplido sin cambios.
Racional: el desarrollador compartió una captura de un gestor de categorías jerárquico (filas con borde, chevron, ícono, punto de estado, indentación sin líneas de conexión) y pidió reproducir ese estilo para carpetas/subcarpetas. Se adaptó la estructura visual (tarjetas, indentación, punto de estado) usando exclusivamente tokens de marca ya aprobados (§C) — el verde del punto es `--color-pine` (ya usado como `--color-success` en el resto de la app), no un color nuevo.
Alternativas consideradas: mantener el padding-left interno y solo agregar el borde/punto de estado (descartado tras comparar con la referencia — el borde no se angostaba con la profundidad, lo cual se veía distinto a la imagen compartida); agregar una línea de conexión vertical estilo árbol de archivos clásico (descartado — el desarrollador pidió explícitamente "sin la línea vertical").
Impacto: `AdminFolderPanel.tsx` — el `<li>` de cada nodo (y su variante en edición/rename) usa `marginLeft: depth === 0 ? 0 : 18` en vez de `paddingLeft` dentro de `.node`; nuevo `<span className={styles.statusDot}>` en cada fila; `folderName` con clase condicional `folderNameTopLevel` en depth 0. `AdminFolderPanel.module.css` — `.node` con borde/fondo/radio nuevos, `.statusDot` y `.folderNameTopLevel` nuevos, `.nodeActive`/`:hover` migrados de fondo tenue a borde `--color-primary`. Sin tests nuevos (cambio puramente visual, la estructura de navegación/expansión no cambió) — 57/57 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Vuelta a filas de solo texto, ícono exclusivo en Raíz, línea vertical por nivel (Incremento 12.11)
════════════════════════════════════════════════════════
Fecha: 2026-07-25
Decisión: Revertir el estilo de tarjeta del Incremento 12.10 — `AdminFolderPanel` vuelve a filas planas de solo texto (sin borde, sin fondo propio, sin punto de estado), con el ícono de carpeta reservado exclusivamente para el ítem "Raíz" (se eliminó `FolderIcon`, que ya no tiene ningún uso). El chevron de expandir/contraer se movió del extremo izquierdo al derecho de la fila. La indentación por nivel pasa de `margin-left` numérico (calculado a partir de `depth`) a anidar un `<ul className={styles.subtree}>` real por cada nivel de hijos, con `border-left: 1px solid var(--color-line)` — cada nivel dibuja su propio segmento de línea vertical de guía por el simple anidamiento del DOM, sin necesitar calcular ni pasar `depth` como parámetro.
Racional: el desarrollador compartió una segunda referencia visual (un nav de documentación tipo árbol de archivos, con líneas de guía y sin tarjetas) y pidió explícitamente ese estilo — texto plano, ícono solo en la raíz, chevron a la derecha, y línea vertical. Esto contradice literalmente el pedido "sin la línea vertical" del Incremento 12.10, pero se prioriza la instrucción más reciente y más específica (una imagen de referencia distinta) sin cuestionar el cambio de dirección — es una preferencia de diseño en evolución, no un error a corregir.
Alternativas consideradas: mantener el punto de estado o el ícono por fila junto con el nuevo estilo de línea vertical (descartado — la referencia compartida no los tiene, y el pedido fue explícito en "solo letras" e "ícono solo en el raíz").
Impacto: `AdminFolderPanel.tsx` — `renderNode` ya no recibe `depth` (innecesario, la indentación es estructural vía anidamiento); se sacó `FolderIcon`, el `<span className={styles.folderIcon}>` con ícono queda solo en el botón de "Raíz"; el chevron pasa a ser el último elemento de la fila. `AdminFolderPanel.module.css` — se sacaron `.folderNameTopLevel`, `.statusDot` y el estilo de tarjeta de `.node` (borde/fondo/radio); nuevo `.subtree` (ul anidado con `border-left`/`margin-left`/`padding-left`) reemplaza el cálculo de indentación en línea. `AdminFolderPanel.test.tsx` sin cambios (los tests ya verificaban comportamiento — navegar, expandir, crear, renombrar, eliminar — no estilos). 57/57 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Raíz contiene visualmente a sus carpetas, chevron SVG único, sin encabezado "Carpetas" (Incremento 12.12)
════════════════════════════════════════════════════════
Fecha: 2026-07-26
Decisión: (1) Las carpetas de nivel superior dejan de ser hermanas de "Raíz" en la misma `<ul>` — se anidan dentro del `<li>` de "Raíz", en un `<ul className={styles.subtree}>` propio, exactamente el mismo mecanismo que usa cualquier carpeta para mostrar sus hijos. Esto hace que la línea vertical de guía (`.subtree`'s `border-left`) conecte visualmente "Raíz" con todo lo que contiene, en vez de quedar como un ítem más de una lista plana. (2) El chevron de texto Unicode (▾ cerrado, ▸ abierto — glifos de tamaño y grosor visualmente inconsistentes entre sí según la fuente) se reemplazó por un único ícono SVG (`ChevronIcon`, flecha `›` de 14px) que rota 90° vía `transform: rotate(90deg)` cuando la carpeta está expandida — mismo ícono, mismo tamaño, solo cambia la rotación. (3) Se eliminó el texto "Carpetas" que encabezaba el panel — quedaba redundante con "Documentos" del header de arriba y con el `aria-label="Carpetas del corpus"` ya presente en el `<nav>`.
Racional: el desarrollador señaló explícitamente que no se notaba que la raíz "contenía" al resto (problema real de jerarquía visual, no solo de gusto), pidió el mismo ícono/tamaño para abierto/cerrado (problema de inconsistencia visual entre los glifos Unicode ▾/▸), y pidió sacar la palabra "Carpetas" por redundante.
Alternativas consideradas: envolver visualmente todo con un borde alrededor de "Raíz" en vez de anidarlo en el árbol (descartado — un borde alrededor no comunica "contención jerárquica" tan claramente como usar el mismo patrón de línea vertical que ya se usa para toda otra relación padre-hijo del árbol); mantener los glifos Unicode pero fijar su `font-size` explícitamente (descartado — un SVG propio da control total de tamaño/grosor/alineación entre ambos estados, en vez de depender de cómo cada fuente del sistema renderiza ▾ y ▸ de forma distinta).
Impacto: `AdminFolderPanel.tsx` — el botón de "Raíz" y su `<ul className={styles.subtree}>` de hijos ahora viven dentro del mismo `<li>` (antes "Raíz" y las carpetas raíz eran `<li>` hermanos sueltos en `.tree`); nuevo componente `ChevronIcon({ expanded })`; se sacó el `<span className={styles.heading}>Carpetas</span>`. `AdminFolderPanel.module.css` — se sacó `.heading`; `.chevron` ahora es flex-container centrado (antes solo texto); nuevas `.chevronIcon`/`.chevronIconExpanded` con la rotación. Sin tests nuevos — los tests existentes verifican comportamiento (expandir/navegar/crear/renombrar/eliminar), no el marcado visual exacto del chevron ni la palabra "Carpetas" (nunca se testeó ese string). 57/57 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — AdminUploadForm rediseñado como barra de ruta estilo GitHub (Incremento 12.13)
════════════════════════════════════════════════════════
Fecha: 2026-07-26
Decisión: Se reemplazó la tarjeta "Subir documento a {carpeta}" (label + "Elegir archivo" + "Subir") por una barra de ruta estilo GitHub: a la izquierda, la ubicación actual como breadcrumb navegable ("Raíz / Presupuestos / 2026 /" — cada carpeta ancestro es un link que navega, la última es texto en negrita, sin link, igual que en GitHub); a la derecha, un único botón "Agregar archivo". Se eliminó el paso intermedio de "elegir archivo y después confirmar" — un solo click abre el selector nativo del sistema operativo y, apenas se elige un archivo válido, la subida arranca automáticamente (el botón cambia a "Subiendo…" y se deshabilita mientras dura). Si el nombre ya existe en el corpus, se sigue bloqueando la subida y mostrando la advertencia — ese comportamiento no cambió.
Racional: el desarrollador compartió una captura de GitHub (`flippy / checks /` a la izquierda de la lista de archivos, botón "Add file" a la derecha) y pidió reemplazar la tarjeta existente por ese patrón exacto: ubicación a la izquierda, acción de subida a la derecha. Un patrón de navegación por ruta (breadcrumb) es más compacto y familiar que repetir el nombre de la carpeta actual en un label de texto plano, y permite además navegar directo a cualquier ancestro sin pasar por el árbol de la izquierda.
Alternativas consideradas: mantener el flujo de dos pasos ("Elegir archivo" + "Subir" como botones separados) y solo cambiar el label de arriba por el breadcrumb (descartado — la imagen de referencia muestra un único control de subida a la derecha, no dos); mostrar el nombre del archivo seleccionado antes de subir como confirmación extra (descartado — al no haber ya un paso de confirmación separado, mostrarlo solo agregaría un parpadeo visual sin aportar control real, dado que la subida ya es automática).
Impacto: `AdminUploadForm.tsx` reescrito — nuevas props `folders`/`currentFolderId`/`onNavigate` reemplazan `currentFolderName` (string plano); nueva función `ancestorChain` arma la cadena de carpetas desde la raíz hasta la carpeta actual a partir de `parent_id`; se sacaron `selectedFileName`, el botón "Elegir archivo" y el botón "Subir" — un solo `<input type="file">` oculto se dispara desde el botón "Agregar archivo", y `handleFileChange` sube el archivo directamente (ya no hay `<form onSubmit>`). `AdminUploadForm.module.css` reescrito con clases de breadcrumb (`.breadcrumb`, `.breadcrumbRoot`, `.breadcrumbLink`, `.breadcrumbCurrent`, `.breadcrumbSep`) y un único `.addButton`. `admin/page.tsx` pasa `folders`/`currentFolderId`/`onNavigate={setCurrentFolderId}` en vez de `currentFolderName`. `AdminUploadForm.test.tsx` reescrito: se sacó el paso de `fireEvent.click(screen.getByText("Subir"))` en los 3 tests existentes (la subida ahora es automática al seleccionar el archivo) y se agregó 1 test nuevo de breadcrumb + navegación. 58/58 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Botón de subida "Subir archivo" con el estilo de "+ Nueva carpeta" (Incremento 12.14)
════════════════════════════════════════════════════════
Fecha: 2026-07-26
Decisión: El botón de subida (Incremento 12.13, "Agregar archivo", sólido en `--color-primary` con texto blanco) pasa a decir "Subir archivo" y adopta el mismo estilo visual que el botón "+ Nueva carpeta" de `AdminFolderPanel`: fondo transparente, borde punteado en `--color-primary`, texto en `--color-primary`, mismo padding/radio/hover (`rgba(139, 46, 59, 0.06)`).
Racional: pedido explícito del desarrollador para que las dos acciones de "agregar contenido" del panel de admin (crear carpeta / subir archivo) compartan el mismo lenguaje visual — un botón "outline" punteado en vez de dos estilos distintos (uno sólido, otro punteado) para acciones de la misma naturaleza.
Alternativas consideradas: mantener el botón sólido pero solo cambiar el texto a "Subir archivo" (descartado — el pedido fue explícito en igualar también el estilo, no solo el texto).
Impacto: `AdminUploadForm.module.css` — `.addButton` reescrito con los mismos valores que `.newFolderButton` de `AdminFolderPanel.module.css` (fondo transparente, borde punteado, sin `font-weight: bold`). `AdminUploadForm.tsx` — el label del botón cambia de "Agregar archivo" a "Subir archivo" (el estado "Subiendo…" durante la subida no cambió). Sin tests nuevos — ningún test verificaba el texto exacto del botón. 58/58 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Zona de arrastrar-y-soltar estilo GitHub con subida múltiple; borde sólido en "Subir archivo" (Incremento 12.15)
════════════════════════════════════════════════════════
Fecha: 2026-07-26
Decisión: (1) El borde de "Subir archivo" pasa de punteado a sólido, manteniendo `--color-primary` como color de borde/texto (mismo color que "+ Nueva carpeta", pero ya no idéntico en el detalle del trazo). (2) El click en "Subir archivo" ahora abre/cierra una zona de arrastrar-y-soltar debajo de la barra de ruta, calcada del patrón "Upload files" de GitHub: ícono de archivo centrado, texto "Arrastrá archivos acá para agregarlos al corpus", y un link "elegí tus archivos" que abre el selector nativo del sistema con `multiple` habilitado. Tanto el drop como la selección múltiple entran al mismo `handleFiles`, que separa los nombres ya existentes en el corpus (bloqueados, con aviso pluralizado "Ya existe un archivo.../Ya existen archivos...") de los válidos, y sube estos últimos uno por uno mostrando progreso ("Subiendo N de M: nombre.pdf"). Si termina sin errores y sin duplicados, el panel se cierra solo; si hubo algún duplicado o falla, queda abierto con el aviso visible.
Racional: pedido explícito del desarrollador con una captura de referencia de GitHub — subida múltiple con arrastrar-y-soltar es más eficiente que un input de un solo archivo cuando se cargan varios documentos del corpus a la vez, y replicar el patrón visual de GitHub (ya usado como referencia para el breadcrumb del Incremento 12.13) mantiene consistencia con esa dirección de diseño elegida por el desarrollador.
Alternativas consideradas: subir todos los archivos en paralelo con `Promise.all` en vez de secuencial (descartado — subir uno por uno permite mostrar progreso legible "N de M" y evita saturar el backend con cargas simultáneas de archivos grandes, sin necesitar un límite de concurrencia); cerrar el panel siempre al terminar aunque haya duplicados (descartado — el usuario necesita ver el aviso de qué archivos quedaron afuera antes de que el panel desaparezca).
Impacto: `AdminUploadForm.tsx` — nuevo estado `isPanelOpen`/`isDragging`/`progress`/`duplicateNames` (antes `duplicateName` singular); nueva función `handleFiles(fileList)` reemplaza la lógica de un solo archivo de `handleFileChange`; nuevos manejadores `handleDrop`/`onDragOver`/`onDragLeave`; nuevo componente `FileIcon`. `AdminUploadForm.module.css` — `.addButton` con `border: 1px solid` (antes `dashed`); nuevas clases `.wrapper`, `.dropzone`, `.dropzoneDragging`, `.dropzoneIcon`, `.dropzoneText`, `.dropzoneSub`, `.chooseLink`, `.progress`. `AdminUploadForm.test.tsx` — los 4 tests existentes ahora abren el panel primero (`fireEvent.click(screen.getByText("Subir archivo"))`) antes de interactuar con el input (que ya no está siempre en el DOM); 1 test nuevo verifica que seleccionar dos archivos dispara `onUpload` dos veces. 59/59 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Cola de archivos + botón de proceso explícito, la subida deja de ser automática (Incremento 12.16)
════════════════════════════════════════════════════════
Fecha: 2026-07-26
Decisión: Se separó "elegir/soltar archivos" de "subirlos a la base". Ahora los archivos elegidos (por drag&drop o por el selector) se agregan a una cola visible — una lista con ícono, nombre de archivo y un botón "✕" para quitarlo — y no se sube nada hasta que el usuario hace click en un botón explícito "Subir N archivo(s)". `handleUploadQueue` recorre la cola secuencialmente, saca de la cola los archivos que subieron bien y deja los que fallaron (mostrando el mismo aviso de error de antes) para poder reintentar sin tener que volver a seleccionarlos.
Racional: el desarrollador compartió una captura más completa del flujo real de GitHub — no es solo la zona de arrastrar-y-soltar (Incremento 12.15), sino que los archivos agregados aparecen en una lista/tabla revisable antes de confirmar el commit. Pidió textualmente que el módulo de subida funcionara así: agregar a una cola, mostrar en tabla, y procesar recién al apretar un botón — dar al usuario la chance de revisar (y sacar) archivos antes de que efectivamente lleguen al backend, en vez de que cada selección dispare una subida irreversible al toque.
Alternativas consideradas: mantener la subida automática por archivo pero agregar la tabla solo como historial de "ya subidos" (descartado — el pedido fue explícito en que el botón de "subir o procesar" sea el que efectivamente sube, es decir la cola es de pendientes, no de subidos); usar el nombre de archivo como key de la cola en vez de un `id` generado (descartado — dos archivos con el mismo nombre elegidos en momentos distintos, o el mismo archivo re-agregado tras sacarlo, colisionarían como key de React sin un identificador único por selección).
Impacto: `AdminUploadForm.tsx` — nueva interfaz `QueuedFile { id, file }` y helper `queueId(file)`; nuevo estado `pendingFiles: QueuedFile[]`; `handleFiles` (Incremento 12.15, subía directo) se reemplaza por `addFiles` (solo valida y encola) + nueva `handleUploadQueue` (sube la cola) + nueva `removeFromQueue(id)`. Nuevo bloque de render: `<ul className={styles.queueList}>` con una fila por archivo pendiente (ícono 16px + nombre + botón "✕" deshabilitado durante la subida) y el botón `.processButton` ("Subir N archivo(s)"), visible solo cuando `pendingFiles.length > 0`. `AdminUploadForm.module.css` — nuevas clases `.queueList`, `.queueRow`, `.queueIcon`, `.queueName`, `.queueRemove`, `.processButton`. `AdminUploadForm.test.tsx` reescrito: los tests ahora verifican que seleccionar archivos NO llama a `onUpload` hasta el click en "Subir N archivos", más un test nuevo de "quitar un archivo de la cola antes de subir". 60/60 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════

════════════════════════════════════════════════════════
📋 DECISIÓN — Tabla de documentos oculta mientras el módulo de subida está abierto (Incremento 12.17)
════════════════════════════════════════════════════════
Fecha: 2026-07-26
Decisión: `AdminUploadForm` notifica a su padre cuándo el panel de subida (dropzone + cola) está abierto o cerrado, vía una nueva prop `onPanelOpenChange`. `admin/page.tsx` usa ese estado (`isUploadPanelOpen`) para no renderizar `AdminDocumentTable` mientras el panel está abierto — la tabla reaparece sola (ya con los documentos nuevos, porque `handleUpload` ya recargaba la lista tras cada subida) apenas el panel se cierra, ya sea porque el usuario lo cerró o porque `handleUploadQueue` lo cerró automáticamente al terminar sin errores.
Racional: con la cola de archivos y el botón de proceso del Incremento 12.16, la columna de contenido creció en altura (dropzone + tabla de pendientes + botón), empujando la tabla de documentos existente hacia abajo sin necesidad real — el desarrollador pidió tratar la subida como un módulo separado que oculte la tabla mientras está en uso, en vez de convivir ambas siempre visibles.
Alternativas consideradas: mantener la tabla visible pero con scroll independiente para no empujar el resto de la página (descartado — no resuelve el pedido explícito de "que quede oculta", solo mitiga el síntoma de layout); duplicar el estado de apertura del panel directamente en `admin/page.tsx` en vez de levantarlo desde `AdminUploadForm` (descartado — el estado de si el panel está abierto es intrínseco al propio componente de subida, que ya lo maneja para su dropzone/cola; notificarlo hacia arriba evita tener dos fuentes de verdad para el mismo booleano).
Impacto: `AdminUploadForm.tsx` — nueva prop opcional `onPanelOpenChange?(isOpen)`; nueva función interna `updatePanelOpen(isOpen)` que centraliza `setIsPanelOpen` + la notificación al padre, usada tanto en el toggle del botón "Subir archivo" como en el cierre automático de `handleUploadQueue` al terminar sin errores. `admin/page.tsx` — nuevo estado `isUploadPanelOpen`, pasado como `onPanelOpenChange={setIsUploadPanelOpen}`; el bloque `{isLoading ? ... : <AdminDocumentTable ... />}` ahora está condicionado a `!isUploadPanelOpen`. Sin tests nuevos — cambio de composición entre `admin/page.tsx` y `AdminUploadForm`, ninguna lógica interna de cada componente cambió. 60/60 tests Jest verdes, build de producción limpio.
════════════════════════════════════════════════════════
